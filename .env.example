# Client env example — copy to `.env` for local dev.
# PUBLIC key only below. This file is safe to commit.

VITE_VAPID_PUBLIC_KEY=BEz91qJkm04v7EWbP3LwiLqtQyFJOiqrKc2s4HgcL-syOlMaY3xESZdcrC2wpLnu2XIfj7lFiHeSlL9f-6V7bMs

# ── Server-only (Vercel dashboard → Project → Settings → Environment
# Variables). NEVER put values here, NEVER commit them. ──
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=mailto:you@example.com
# KV_REST_API_URL=        (auto-set when a Vercel KV store is linked)
# KV_REST_API_TOKEN=      (auto-set when a Vercel KV store is linked)
